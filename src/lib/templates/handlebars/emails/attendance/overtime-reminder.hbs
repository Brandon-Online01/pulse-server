{{#base headerTitle="Overtime Work Reminder" headerSubtitle="You're working beyond scheduled hours"}}

<div class="greeting">
    Hello {{employeeName}}! ⏰
</div>

<div class="warning-notice">
    <p>🕐 <strong>Overtime Notice:</strong> You're currently working beyond your scheduled shift hours. This time will be automatically calculated as overtime.</p>
</div>

<div class="message">
    <p>Your scheduled shift ended at <strong>{{organizationCloseTime}}</strong>, but you're still clocked in. We wanted to notify you that you're now working overtime hours.</p>
</div>

<div class="info-card">
    <h3>⏱️ Current Shift Summary</h3>
    <div class="detail-grid">
        <div class="detail-item">
            <span class="detail-label">Check-in Time</span>
            <span class="detail-value">{{checkInTime}}</span>
        </div>
        <div class="detail-item">
            <span class="detail-label">Scheduled End Time</span>
            <span class="detail-value">{{organizationCloseTime}}</span>
        </div>
        <div class="detail-item">
            <span class="detail-label">Current Time</span>
            <span class="detail-value">{{currentTime}}</span>
        </div>
        <div class="detail-item">
            <span class="detail-label">Total Shift Duration</span>
            <span class="detail-value">{{shiftDuration}}</span>
        </div>
        <div class="detail-item">
            <span class="detail-label">Overtime Duration</span>
            <span class="detail-value" style="color: #f59e0b; font-weight: 700;">{{overtimeDuration}}</span>
        </div>
        {{#if breakDetails}}
        <div class="detail-item">
            <span class="detail-label">Total Break Time</span>
            <span class="detail-value">{{breakDetails.totalBreakTime}}</span>
        </div>
        {{/if}}
    </div>
</div>

<div class="info-card" style="background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%); border-left-color: #f59e0b;">
    <h4 style="color: #92400e;">💡 What This Means</h4>
    <ul style="margin-left: 20px; color: #92400e; line-height: 1.8;">
        <li><strong>Overtime Tracking:</strong> All time worked beyond {{organizationCloseTime}} will be automatically recorded as overtime</li>
        <li><strong>No Action Required:</strong> You don't need to do anything special - the system will handle the overtime calculation</li>
        <li><strong>Clock Out When Ready:</strong> Simply clock out when you've completed your work</li>
        <li><strong>Payroll Impact:</strong> Overtime hours will be reflected in your timesheet for payroll processing</li>
    </ul>
</div>

<div class="cta-section">
    <a href="{{clockOutUrl}}" class="cta-button">Clock Out Now</a>
    <p style="margin-top: 16px; font-size: 14px; color: #6b7280;">
        Or continue working - we'll keep tracking your overtime hours accurately.
    </p>
</div>

<div class="message">
    <p style="font-size: 14px; color: #6b7280; margin-top: 30px; padding-top: 20px; border-top: 1px solid #e5e7eb;">
        <strong>Organization:</strong> {{organizationName}}<br>
        <strong>Notification sent:</strong> {{currentTime}}<br>
        <strong>Need help?</strong> <a href="{{dashboardUrl}}" style="color: #7c2d92;">Visit your dashboard</a>
    </p>
</div>

{{/base}} 