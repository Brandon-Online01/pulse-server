export enum TokenType {
    ACCESS = 'access',
    REFRESH = 'refresh',
}

export enum TokenStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    EXPIRED = 'expired',
    REVOKED = 'revoked',
}