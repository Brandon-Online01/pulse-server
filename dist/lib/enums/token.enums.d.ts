export declare enum TokenType {
    ACCESS = "access",
    REFRESH = "refresh"
}
export declare enum TokenStatus {
    ACTIVE = "active",
    INACTIVE = "inactive",
    EXPIRED = "expired",
    REVOKED = "revoked"
}
